
import React from 'react';
import NavbarMenu from './NavbarMenu';
import Footer from './Footer';

const Contato = () => {
  return (
    <div>
      <NavbarMenu />
      {/* Adicione outros componentes necessários para a página de Contato */}
      <h1>Contato</h1>
      {/* Conteúdo da página de Contato */}
      <Footer />
    </div>
  );
};

export default Contato;
